import React from 'react';
import clsx from 'clsx';

function AIChatWidget({ className }) {
  return (
    <div className={clsx('fixed bottom-4 left-4 p-4 bg-primary-800 text-white rounded-lg shadow-lg', className)}>
      <p>AI Chat Widget (Coming Soon)</p>
      {/* Placeholder for chat interface */}
    </div>
  );
}

export default AIChatWidget;
