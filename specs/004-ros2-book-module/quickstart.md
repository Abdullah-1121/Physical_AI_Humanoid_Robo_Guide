# Quickstart: Book Module 1 — Robotic Nervous System

To view the new documentation module, follow these steps:

1.  **Build the Docusaurus site**:
    ```bash
    npm install
    npm run build
    ```
2.  **Serve the site locally**:
    ```bash
    npm run serve
    ```
3.  **Navigate to the module pages**: Once the site is running, open your browser and navigate to the following URLs (replace `localhost:3000` with your local server address):
    -   `http://localhost:3000/docs/module-1/ros2-middleware-robot-control`
    -   `http://localhost:3000/docs/module-1/ros2-nodes-topics-services`
    -   `http://localhost:3000/docs/module-1/python-agents-ros-controllers`
    -   `http://localhost:3000/docs/module-1/urdf-humanoids`

You should see the lesson content along with the "Personalize" and "Translate to Urdu" button placeholders on each page.
